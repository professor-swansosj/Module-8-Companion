# TODO: Complete this production-ready Dockerfile
# Hint: Use Python slim image, install dependencies, expose port

# TODO: Use Python base image
FROM python:3.11-slim

# TODO: Set working directory
WORKDIR /app

# TODO: Copy requirements and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# TODO: Copy application code
COPY network_api.py .

# TODO: Expose the port FastAPI will run on
# Hint: Use EXPOSE 8000
EXPOSE 

# TODO: Set the command to run the FastAPI server
# Hint: Use CMD ["uvicorn", "network_api:app", "--host", "0.0.0.0", "--port", "8000"]
CMD 